using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HOVER : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //time.timescale = 0 pausa = 1 play
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
